import { AutoChangeDirective } from './auto-change.directive';

describe('AutoChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoChangeDirective();
    expect(directive).toBeTruthy();
  });
});
